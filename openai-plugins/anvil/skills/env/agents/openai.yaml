interface:
  display_name: "Anvil: Env"
  short_description: "Create or update a devenv development environment for the current repo. Reads the devenv docs before writing, every time."
policy:
  allow_implicit_invocation: false
