interface:
  display_name: "Temper: Audit"
  short_description: "Repo-wide security audit across secrets, dependencies, code at trust boundaries, and configuration. Read-only, ranked by exploitability."
policy:
  allow_implicit_invocation: false
