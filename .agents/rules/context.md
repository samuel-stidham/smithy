# smithy working instructions

Follow the canonical agent instructions in @/.agents/context.md
(the `.agents/context.md` file at the repo root). That file is the
single source of truth. Do not add instructions here.
